# ===== Local development environment variables =====
# Postgres
POSTGRES_DB=a2eg_dsa
POSTGRES_USER=a2eg_user
POSTGRES_PASSWORD=supersecret

# Compose-only DATABASE_URL used by backend service for local dev
# Connect to the db service name on docker network
DATABASE_URL=postgresql://a2eg_user:supersecret@db:5432/a2eg_dsa

# Backend CORS allowed origin
FRONTEND_URL=http://localhost:3000

# Vite frontend uses this to call backend
VITE_BACKEND_URL=http://localhost:8000

# Optional external API keys
LLM_API_KEY=
